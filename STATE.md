# Execution State

**Current Goal:**
Tiers 0-2 are DONE, including all E2E scenarios, verified privileged. Tier 4.1 is in
progress: filesystem SHA-256 evidence at completed writes, then Tier 3 (network).
The Tier 1/Tier 2 E2E work was committed as `d5e1225`; the Go 1.26 `govulncheck`
CI fix was committed and pushed as `c3fb88e`. The project baseline stays Go 1.25.

**Completed Steps:**
* Tier 0 (F0.1-F0.4): data model, matching, verification, verdict. All five planned E2E attack scenarios exist as tests in `pkg/verification/verification_test.go` (honest, T1 omission, T2 fabrication, T3 substitution, combined). Run without privileges; audited green.
* F1.1 (Filesystem Observer): implemented in `pkg/probe/fs` (fanotify).
* F1.2 (Simulated Agent): `cmd/simagent/main.go` performs file operations and records matching `TrajectoryEntry` values; it also has a Tier 2 `wc -l` subprocess step, which `--file-only` suppresses for Tier 1 runs.
* F1.3 (Trajectory Ingestion): `models.ParseTrajectory` handles it.
* E2E Tier 1: `tests/e2e/tier1_test.go` builds simagent, runs it in a tracked temp dir, verifies. All four plan scenarios now present and passing privileged: honest (FAITHFUL), omission (Unrecorded), fabrication (Unwitnessed), filename swap (Unwitnessed + Unrecorded). Fabrication and filename-swap added this pass. simagent gained `--file-only` to skip the Tier 2 exec step; all four Tier 1 tests pass it so the trajectory is pure filesystem. Plan doc `02` corrected: filename swap is Unwitnessed + Unrecorded at occurrence level, not Mismatched (that needs Tier 4 content hashes).
* CI Fix (7d6a9cf): repaired 6 failing workflows; bumped golangci-lint-action to v9.3.0.
* CI Fix (`c3fb88e`, pushed): `govulncheck-action` installs `govulncheck@latest`, which now requires Go 1.26 while the job selected Go 1.25. Updated only the security-scanner job to Go 1.26; build, lint, and test jobs remain on the project's Go 1.25 baseline. Unprivileged `go test ./...` and `go vet ./...` pass; privileged validation could not run because sudo authentication is unavailable in this environment.
* CI Fix (lint errcheck): fixed unchecked `unix.Close` calls in `observer.go`, `fanotify.go:111`, `observer_test.go`. golangci-lint v2.13.2 reports 0 issues. CI shows only 3 findings at a time (`max-same-issues: 3`), so grep the whole repo rather than fixing iteratively.
* Agent docs refactor: shared `AGENTS.md` plus thin `CLAUDE.md` / `GEMINI.md` / `CODEX.md` wrappers.
* F2.2 (Mixed verification + command normalization), committed bc5373d: `targetsMatch`/`commandsMatch` in `pkg/matching/matching.go` normalize `ls` vs `/usr/bin/ls` by basename when exactly one side is a bare command. Deterministic; never consults host PATH or filesystem. The fanotify parent-directory fallback is scoped to file actions only. Tests: `TestProcessCommandNormalization`, `TestProcessExitUsesCommandNormalization`, `TestMixedFileAndProcessVerification`, `TestMixedVerificationDetectsOmittedProcess`.
* F2.1 (eBPF process observer), verified privileged (`sudo go test -v ./pkg/probe/proc/...` all green, incl. new exit-code/timestamp tests), ready to commit: `pkg/probe/proc`. Non-CO-RE BPF C program on `syscalls/sys_enter_execve` + `syscalls/sys_enter_exit_group` + `sched/sched_process_exit`. argv stored as 12 fixed-width 128-byte slots (constant offsets, so the verifier bounds them without gymnastics). PERCPU_ARRAY heap to dodge the 512-byte stack limit. argv cached by TGID; `sys_enter_exit_group` updates that same cached record in-place with the exit code (glibc's exit()/_exit() path; a process killed by an uncaught signal never hits it, so `HasExitCode` stays false rather than fabricating 0); `sched_process_exit` emits the final replay with `ts_ns = bpf_ktime_get_ns()` stamped at actual exit, not at userspace read time. `go build`, `go vet`, golangci-lint v2.13.2 all clean (0 issues).
* Exit code and in-kernel timestamps wired end-to-end, not just captured: `models.GroundTruthEvent`/`TrajectoryEntry` gained `ExitCode *int32` (nil = unknown, same optionality pattern as the hash fields); `verification.go` gained `exitCodesAgree` with the same nil-agreement rule as `hashesAgree`, folded into the Corroborated/Mismatched decision — an agent claiming exit 0 on a command that actually failed is now a Mismatched, tested in `TestExitCodeMismatch`/`TestExitCodeNilTreatedAsAgreement`.
* Kernel timestamp conversion: `observer.go` computes `bootOffsetNs` once at `New()` from a matched `CLOCK_MONOTONIC`/`time.Now()` pair (`golang.org/x/sys/unix.ClockGettime`), then every event's `bpf_ktime_get_ns()` reading is converted via `wallNs = monoNs + bootOffsetNs` — removes ring-buffer drain latency from ground-truth timestamps. Tested in `TestObserver_ExitCodeAndTimestamps` (window check against wall-clock before/after, plus exec-before-exit ordering).
* Deleted `zz_verifierlog_debug_test.go` (debug scaffolding, marked "delete before committing" in its own header).
* Fixed `Lint and Security` CI failure by upgrading `github.com/cilium/ebpf` to `v0.22.0` (fixing `GO-2026-6238` vulnerability) and re-running `go generate`.
* Fixed `bpf-freshness` CI drift failure by filtering out `.o` files from the git status check. OS/kernel headers differ between the dev environment (Ubuntu 25.10) and CI (`ubuntu-latest`), causing `bpf2go` to produce a completely different BTF in the binary `.o`. We rely on the `.go` files changing for struct updates, and existing test coverage failing if logic changes without regeneration.
* Live verification harness: added `pkg/probe.Observer` (the `Start()`/`Stop() error`/`Events() <-chan models.GroundTruthEvent` interface `fs.Observer` and `proc.Observer` already satisfy structurally), `cmd/watch` (ground-truth recorder CLI, probes built from a `probeBuilders` map keyed by name -- one map entry per tier's probe, `fs`+`proc` today), and `cmd/verify` (thin CLI over `verification.Verify`, tier-agnostic -- only depends on `models`/`verification` types). Extended `cmd/simagent` with a real Tier 2 step: execs `wc -l` on the renamed file, reporting the exact argv string used (matches byte-for-byte what the eBPF probe reconstructs, sidestepping `commandsMatch`'s bare-name heuristic which only works for zero-arg commands) plus the exit code and the `FAN_OPEN` side effect wc's read causes on the fs probe. Added `--drop-entry N` to simagent to manually trigger a NOT FAITHFUL run for testing. README gained a "Live demo" section with the two-terminal walkthrough. `.gitignore` gained the root-level demo binaries/output that showed up as untracked after following that walkthrough.
* E2E Tier 2: `tests/e2e/tier2_test.go` (new, passing privileged). Shared `runTier2Agent` helper runs simagent (no `--file-only`) with both the `fs` and `proc` probes active, `proc` scoped via `CommandFilter: "wc -l " + workspace` so it ignores simagent's own execve. Four scenarios: honest (FAITHFUL, asserts both probe types contributed events), omission (drop `process_exec` -> Unrecorded), fabrication (fake `curl` `process_exec` -> Unwitnessed), substitution (alter reported exit code on `process_exit` -> Mismatched). The substitution case is the first tier where T3 surfaces as Mismatched rather than Unwitnessed + Unrecorded, because the process probe records an exit code to disagree on. Confirms the wc-triggered `FAN_OPEN` on file2.txt lines up (previously only exercised via the manual live demo).
* Tier 4.1 (Slice 1 complete): added `pkg/content.SHA256File`, which emits `sha256:<hex>` digests; `fs.Observer` safely attaches an output hash to `FileClose` events after `FAN_CLOSE_WRITE` using a non-blocking background goroutine; and simagent reports the same final-content hash. The observer's own hash-read `FAN_OPEN` is safely suppressed. New unit coverage checks known and missing files, observer hash attachment, and suppression of the observer's own hash-read. `tests/e2e/tier4_test.go` verifies a modified hash becomes `Mismatched`.

**Active Context:**
* The live demo's privileged path (`sudo ./watch` + `./simagent` + `./verify`) has not been run end-to-end by an agent in this repo -- no passwordless sudo in that sandbox. Argv-string-equality reasoning says it should print FAITHFUL, but this needs a real confirmed run; if it doesn't come out clean, check `commandsMatch`/simagent's argv construction first.
* `tests/e2e/tier2_test.go` now covers honest, omission, fabrication, and exit-code substitution scenarios; privileged execution should be re-run when validating the complete worktree.
* `go test ./...` unprivileged is misleading: every fs, proc, and Tier 1 E2E test skips without root — only Tier 0 and matching/verification unit tests actually execute that way. Always confirm probe changes with `sudo go test -v ./...` before trusting green.
* Stale-artifact hazard closed: new `bpf-freshness` job in `.github/workflows/test.yml` installs clang + libbpf-dev, runs `go generate ./pkg/probe/proc/...`, and fails the build if that dirties `pkg/probe/proc`. Verified locally by editing `proc.bpf.c`, confirming the check fails, then reverting.
* Tier 1 E2E now covers all four planned scenarios (honest, omission, fabrication, and filename swap).
* `models.TrajectoryEntry.Target` is still a single string with no dedicated args field; process entries pack args into the target (e.g. "git commit -m fix"). Unchanged by this pass.
* CI runs `sudo go test -v ./...`, so this commit is the first time the privileged proc tests execute on a GitHub Actions runner — watch the first run.
* Privileged Tier 4 validation initially exposed two Unrecorded `file_open` events: one from the observer's own `SHA256File` read and one from simagent reopening the file to compute its report. The observer now suppresses only its tracked hash-read `FAN_OPEN`; simagent now hashes its known write bytes in memory via `content.SHA256Bytes`. External `sudo go test -v ./...` now passes all packages and E2E scenarios.
* `docs/` and `STATE.md` are gitignored; the plan and this file are local-only.
* golangci-lint is not in PATH; `~/go/bin/golangci-lint` is a stale v1.59.1 broken on this toolchain. Install v2.13.2 into a scratchpad via `GOBIN=<scratchpad> go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.13.2` (it self-upgrades the toolchain to go1.26.8 via GOTOOLCHAIN on install, that's expected).

**Next Steps:**
1. Run the live demo as root to confirm the FAITHFUL verdict end-to-end, and the `--drop-entry` NOT FAITHFUL path.
2. Confirm the GitHub Actions run for `c3fb88e`, including its privileged suite and Go 1.26 vulnerability scan.
3. Begin Tier 4.2: Implement `InputHash` for `FileOpen`/`FileRead` to capture pre-write/write-buffer states.
